---
title: "PERMANOVA"
format: html
---

## Library and Data

```{r}
#| label: library_load

library(tidyverse)
library(knitr)
library(readxl)
library(data.table)
library(vegan)
```

```{r}
#| label: read_csvs

Corals_clean <- read.csv("/Users/ellaandonov/World_Corals/Cleaned data CSVs/Corals_clean.csv")
ITS2_clean   <- read.csv("/Users/ellaandonov/World_Corals/Cleaned data CSVs/ITS2_clean.csv")
met_summary_coral_only <- read.csv("/Users/ellaandonov/World_Corals/Metabolite Summary Data/met_summary_coral_only.csv")
```

```{r}
#| label: install_packages

install.packages("remotes")
remotes::install_github("pmartinezarbizu/pairwiseAdonis/pairwiseAdonis")
```

## Metadata PERMANOVAs and PCoAs

```{r}
#| label: permanova_phylum

# Select only numeric metabolite columns
permanova_numeric_data <- Corals_clean[, sapply(Corals_clean, is.numeric)]

# Compute Bray-Curtis distance
bray_curtis <- vegdist(permanova_numeric_data, method = "bray")

# Run PERMANOVA
phylum_permanova_result <- adonis2(bray_curtis ~ host_phylum, data = Corals_clean, permutations = 999)

# Display results
print(phylum_permanova_result)
```

```{r}
#| label: PCoA_phylum_confidence_ellipses

# Select metabolite columns (columns starting with "x")
met_cols <- grep("^x", names(Corals_clean), value = TRUE)

# numeric matrix of metabolite abundances (same selection as PERMANOVA)
met_mat <- as.matrix(Corals_clean[, met_cols])
mode(met_mat) <- "numeric"

# Bray–Curtis distance calculated from numeric abundances (same as PERMANOVA)
bc_dist <- vegdist(met_mat, method = "bray")

# --- PCoA (classical MDS)
pcoa <- cmdscale(bc_dist, eig = TRUE, k = 2)

# calculate percent variance explained (only positive eigenvalues)
pos_eigs <- pcoa$eig[pcoa$eig > 0]
var_explained <- round(100 * pcoa$eig[1:2] / sum(pos_eigs), 1)

# build plotting data frame (keep row order consistent with met_mat / bc_dist)
pcoa_points_phylum <- data.frame(
  PCoA1 = pcoa$points[, 1],
  PCoA2 = pcoa$points[, 2],
  host_phylum = Corals_clean$host_phylum,
  row.names = rownames(Corals_clean)
)

# --- helper for ellipse computation (unchanged)
veganCovEllipse <- function(cov, center, scale = 1, npoints = 100) {
  theta <- seq(0, 2 * pi, length.out = npoints)
  circle <- cbind(cos(theta), sin(theta))
  ed <- eigen(cov)
  vect <- ed$vectors %*% diag(sqrt(ed$values)) %*% t(circle) * scale
  t(vect + center)
}

# compute ellipses per host_phylum
ellipses_df <- do.call(rbind, lapply(unique(na.omit(pcoa_points_phylum$host_phylum)), function(g) {
  pts <- subset(pcoa_points_phylum, host_phylum == g)[, c("PCoA1", "PCoA2")]
  if (nrow(pts) < 3) return(NULL)
  cov_mat <- cov(pts)
  center <- colMeans(pts)
  el <- veganCovEllipse(cov_mat, center, scale = sqrt(qchisq(0.95, df = 2)), npoints = 200)
  data.frame(PCoA1 = el[, 1], PCoA2 = el[, 2], host_phylum = g)
}))

# --- Plot
ggplot(pcoa_points_phylum, aes(x = PCoA1, y = PCoA2, color = host_phylum)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_polygon(data = ellipses_df,
               aes(x = PCoA1, y = PCoA2, fill = host_phylum),
               alpha = 0.15, colour = NA) +
  labs(
    x = paste0("PCoA 1 (", var_explained[1], "%)"),
    y = paste0("PCoA 2 (", var_explained[2], "%)"),
    title = "PCoA of Bray–Curtis Dissimilarity by Phylum",
    color = "Phylum",
    fill = "Phylum"
  ) +
  theme_minimal(base_size = 14) +
  theme(legend.position = "right")
```

```{r}
#| label: PCoA_phylum_confidence_ellipses_blue

ggplot(pcoa_points_phylum, aes(x = PCoA1, y = PCoA2, color = host_phylum)) +
  geom_point(size = 3, alpha = 0.9) +
  geom_polygon(
    data = ellipses_df,
    aes(x = PCoA1, y = PCoA2, fill = host_phylum),
    alpha = 0.20, colour = NA
  ) +
  labs(
    x = paste0("PCoA 1 (", var_explained[1], "%)"),
    y = paste0("PCoA 2 (", var_explained[2], "%)"),
    title = "PCoA of Bray–Curtis Dissimilarity by Phylum",
    color = "Phylum",
    fill = "Phylum"
  ) +
  theme_minimal(base_size = 14) +
  theme(
    # background colors
    plot.background = element_rect(fill = "#000042", color = NA),
    panel.background = element_rect(fill = "#000042", color = NA),
    
    # text and axis colors
    text = element_text(color = "white"),
    axis.text = element_text(color = "white"),
    axis.title = element_text(color = "white"),
    plot.title = element_text(color = "white", hjust = 0.5),  # << centered title

    # gridlines
    panel.grid.major = element_line(color = "#444477"),
    panel.grid.minor = element_line(color = "#333366"),

    # legend
    legend.background = element_rect(fill = "#000042", color = NA),
    legend.text = element_text(color = "white"),
    legend.title = element_text(color = "white"),

    legend.position = "right"
  ) +
  scale_color_brewer(palette = "Set2") +
  scale_fill_brewer(palette = "Set2")
```

```{r}
#| label: permanova_location

# 1) select numeric columns (metabolites)
permanova_numeric_data <- Corals_clean[, sapply(Corals_clean, is.numeric)]

# 2) find rows with non-missing location AND non-missing numeric values
keep_rows <- !is.na(Corals_clean$location) &
             apply(permanova_numeric_data, 1, function(x) !any(is.na(x)))

# 3) subset both data objects
permanova_numeric_data2 <- permanova_numeric_data[keep_rows, , drop = FALSE]
meta2 <- Corals_clean[keep_rows, , drop = FALSE]

# 4) compute Bray-Curtis on the filtered numeric table
bray_curtis2 <- vegdist(permanova_numeric_data2, method = "bray")

# 5) run PERMANOVA with matching metadata
phylum_permanova_result <- adonis2(bray_curtis2 ~ location, data = meta2, permutations = 999)

# 6) show result
print(phylum_permanova_result)
```

```{r}
#| label: PCoA_location

# PCoA (cmdscale) and percent variance explained
pcoa_result <- cmdscale(bray_curtis2, eig = TRUE, k = 2)
pcoa_points <- as.data.frame(pcoa_result$points)
colnames(pcoa_points) <- c("PCoA1", "PCoA2")
pcoa_points <- bind_cols(pcoa_points, meta2)

pos_eig <- pcoa_result$eig[pcoa_result$eig > 0]
var_explained <- round(100 * pcoa_result$eig[1:2] / sum(pos_eig), 1)

#  Plot with 95% confidence ellipses
ggplot(pcoa_points, aes(x = PCoA1, y = PCoA2, color = location, fill = location)) +
  geom_point(size = 3, alpha = 0.8) +
  stat_ellipse(geom = "polygon", alpha = 0.15, level = 0.95, type = "t", colour = NA) +
  labs(
    title = "PCoA of Bray–Curtis Dissimilarity by Location",
    x = paste0("PCoA 1 (", var_explained[1], "%)"),
    y = paste0("PCoA 2 (", var_explained[2], "%)"), 
    fill = "Location", 
    color = "Location"
  ) +
  theme_minimal(base_size = 14) +
  theme(
    legend.position = "right",
    panel.grid = element_blank(),
    plot.title = element_text(face = "bold")
  )
```

```{r}
#| label: permanova_bleaching

# Select only numeric columns (assumed metabolites)
permanova_numeric_data <- Corals_clean[, sapply(Corals_clean, is.numeric)]

# Build logical mask: non-missing bleaching, no NA in numeric data, and not all zeros
mask_non_missing_meta <- !is.na(Corals_clean$bleaching)
mask_no_numeric_NA <- apply(permanova_numeric_data, 1, function(x) !any(is.na(x)))
mask_not_all_zero <- rowSums(permanova_numeric_data, na.rm = TRUE) > 0

keep_rows <- mask_non_missing_meta & mask_no_numeric_NA & mask_not_all_zero

# Subset numeric table and metadata to matching rows
permanova_numeric_data2 <- permanova_numeric_data[keep_rows, , drop = FALSE]
meta2 <- Corals_clean[keep_rows, , drop = FALSE]

# Compute Bray–Curtis distance on filtered numeric data
bray_curtis2 <- vegdist(permanova_numeric_data2, method = "bray")

# Run PERMANOVA (bleaching)
bleaching_permanova_result <- adonis2(bray_curtis2 ~ bleaching, data = meta2, permutations = 999)

print(bleaching_permanova_result)
```

```{r}
#| label: PCoA_bleaching

# PCoA (cmdscale) and percent variance explained
pcoa_result <- cmdscale(bray_curtis2, eig = TRUE, k = 2)
pcoa_points <- as.data.frame(pcoa_result$points)
colnames(pcoa_points) <- c("PCoA1", "PCoA2")
pcoa_points <- bind_cols(pcoa_points, meta2)

pos_eig <- pcoa_result$eig[pcoa_result$eig > 0]
var_explained <- round(100 * pcoa_result$eig[1:2] / sum(pos_eig), 1)

# Plot with 95% confidence ellipses
p <- ggplot(pcoa_points, aes(x = PCoA1, y = PCoA2, color = bleaching, fill = bleaching)) +
  geom_point(size = 3, alpha = 0.8) +
  stat_ellipse(geom = "polygon", alpha = 0.15, level = 0.95, type = "t", colour = NA) +
  labs(
    title = "PCoA of Bray–Curtis Dissimilarity by Bleaching Status",
    x = paste0("PCoA1 (", var_explained[1], "%)"),
    y = paste0("PCoA2 (", var_explained[2], "%)"), 
    fill = "Bleaching Status", 
    color = "Bleaching Status"
  ) +
  theme_minimal(base_size = 13) +
  theme(legend.position = "right")

print(p)
```

```{r}
#| label: nest_permanova_bleaching_location

# Remove rows with NA in grouping variables and ensure rows match numeric table
keep_rows <- complete.cases(meta2$bleaching, meta2$location)
if(any(!keep_rows)) message(sum(!keep_rows), " rows removed because bleaching or location is NA.")
meta3 <- meta2[keep_rows, , drop = FALSE]
permanova_numeric_data3 <- permanova_numeric_data2[rownames(meta3), , drop = FALSE]  # ensure same order

# Require at least two levels for tests
meta3$bleaching <- factor(meta3$bleaching)
meta3$location <- factor(meta3$location)
if(nlevels(meta3$bleaching) < 2) stop("Need >= 2 bleaching levels to run PERMANOVA.")
if(nlevels(meta3$location) < 2) message("Warning: < 2 location levels — location tests may be meaningless.")

# Recompute Bray–Curtis on the filtered numeric data
bray_curtis3 <- vegdist(permanova_numeric_data3, method = "bray")

# Nested adonis2 model: location nested within bleaching
nested_permanova_result <- adonis2(bray_curtis3 ~ bleaching/location,
                                   data = meta3,
                                   permutations = 999)
print(nested_permanova_result)

# Explicit equivalent
nested_permanova_result_explicit <- adonis2(bray_curtis3 ~ bleaching + bleaching:location,
                                            data = meta3,
                                            permutations = 999)
print(nested_permanova_result_explicit)
```

```{r}
#| label: PCoA_bleaching_and_location

# PCoA (cmdscale) and percent variance explained
pcoa_result <- cmdscale(bray_curtis2, eig = TRUE, k = 2)
pcoa_points <- as.data.frame(pcoa_result$points)
colnames(pcoa_points) <- c("PCoA1", "PCoA2")
pcoa_points <- bind_cols(pcoa_points, meta2)

pos_eig <- pcoa_result$eig[pcoa_result$eig > 0]
var_explained <- round(100 * pcoa_result$eig[1:2] / sum(pos_eig), 1)

# ---- REMOVE SAMPLES WITH NA FOR LOCATION ----
pcoa_points <- pcoa_points |> 
  dplyr::filter(!is.na(location))

# Add shape aesthetic + legend for location
p <- ggplot(pcoa_points, aes(x = PCoA1, y = PCoA2, color = bleaching, fill = bleaching)) +
  geom_point(aes(shape = location), size = 3, alpha = 0.8) +   # <-- added shape
  stat_ellipse(geom = "polygon", alpha = 0.15, level = 0.95, type = "t", colour = NA) +
  labs(
    title = "PCoA of Bray–Curtis Dissimilarity by Bleaching Status & Location",
    x = paste0("PCoA1 (", var_explained[1], "%)"),
    y = paste0("PCoA2 (", var_explained[2], "%)"), 
    fill = "Bleaching Status", 
    color = "Bleaching Status",
    shape = "Location"          # <-- added legend title
  ) +
  theme_minimal(base_size = 13) +
  theme(legend.position = "right")

print(p)
```

```{r}
#| label: permanova_symbiont_potential

# Select numeric columns (assumed metabolites)
permanova_numeric_data <- Corals_clean[, sapply(Corals_clean, is.numeric)]

# Build mask: non-missing predictor, no NA in numeric columns, not all zeros
mask_non_missing_predictor <- !is.na(Corals_clean$symbiont.potential)
mask_no_numeric_NA <- apply(permanova_numeric_data, 1, function(x) !any(is.na(x)))
mask_not_all_zero <- rowSums(permanova_numeric_data, na.rm = TRUE) > 0

keep_rows <- mask_non_missing_predictor & mask_no_numeric_NA & mask_not_all_zero

# Subset numeric table and metadata (ensure same order)
permanova_numeric_data2 <- permanova_numeric_data[keep_rows, , drop = FALSE]
meta2 <- Corals_clean[keep_rows, , drop = FALSE]

# Compute Bray–Curtis on filtered numeric data
bray_curtis2 <- vegdist(permanova_numeric_data2, method = "bray")

# Run PERMANOVA (symbiont.potential)
symbiont_permanova_result <- adonis2(bray_curtis2 ~ symbiont.potential, data = meta2, permutations = 999)

print(symbiont_permanova_result)
```

```{r}
#| label: PCoA_symbiont_potential

# PCoA and 95% confidence ellipses for visualization
pcoa_result <- cmdscale(bray_curtis2, eig = TRUE, k = 2)
pcoa_points <- as.data.frame(pcoa_result$points)
colnames(pcoa_points) <- c("PCoA1", "PCoA2")
pcoa_points <- bind_cols(pcoa_points, meta2)

# positive eigenvalues for % variance
pos_eig <- pcoa_result$eig[pcoa_result$eig > 0]
var_explained <- round(100 * pcoa_result$eig[1:2] / sum(pos_eig), 1)

ggplot(pcoa_points, aes(x = PCoA1, y = PCoA2, color = symbiont.potential, fill = symbiont.potential)) +
  geom_point(size = 3, alpha = 0.8) +
  stat_ellipse(geom = "polygon", alpha = 0.15, level = 0.95, type = "t", colour = NA) +
  labs(
    title = "PCoA of Bray–Curtis by Symbiont Potential",
    x = paste0("PCoA1 (", var_explained[1], "%)"),
    y = paste0("PCoA2 (", var_explained[2], "%)"), 
    fill = "Symbiont Potential", 
    color = "Symbiont Potential"
  ) +
  theme_minimal(base_size = 13) +
  theme(legend.position = "right")
```

```{r}
#| label: nest_permanova_symbiont_location

# 1) select numeric columns (metabolites)
permanova_numeric_data4 <- Corals_clean[, sapply(Corals_clean, is.numeric)]

# 2) find rows with:
#    - non-missing location
#    - non-missing symbiont.potential
#    - no missing numeric values
keep_rows4 <- !is.na(Corals_clean$location) &
              !is.na(Corals_clean$symbiont.potential) &
              apply(permanova_numeric_data4, 1, function(x) !any(is.na(x)))

# 3) subset both data objects
permanova_numeric_data4 <- permanova_numeric_data4[keep_rows4, , drop = FALSE]
meta4 <- Corals_clean[keep_rows4, , drop = FALSE]

# 4) make sure grouping variables are factors
meta4$symbiont.potential <- factor(meta4$symbiont.potential)
meta4$location <- factor(meta4$location)

# 5) compute Bray–Curtis on the filtered numeric table
bray_curtis4 <- vegdist(permanova_numeric_data4, method = "bray")

# 6) nested PERMANOVA: location nested within symbiont.potential
nested_permanova_result_1 <- adonis2(
  bray_curtis4 ~ symbiont.potential/location,
  data = meta4,
  permutations = 999
)
print(nested_permanova_result_1)

# 7) explicit equivalent (main effect + interaction)
nested_permanova_result_explicit_1 <- adonis2(
  bray_curtis4 ~ symbiont.potential + symbiont.potential:location,
  data = meta4,
  permutations = 999
)
print(nested_permanova_result_explicit_1)
```

```{r}
#| label: PCoA_symbiont_and_location

# PCoA (cmdscale) on the SAME Bray–Curtis matrix used in the nested PERMANOVA
pcoa_result_1 <- cmdscale(bray_curtis4, eig = TRUE, k = 2)

# Turn scores into a data frame
pcoa_points_1 <- as.data.frame(pcoa_result_1$points)
colnames(pcoa_points_1) <- c("PCoA1", "PCoA2")

# Bind metadata (meta4 was created in the PERMANOVA chunk)
pcoa_points_1 <- dplyr::bind_cols(pcoa_points_1, meta4)

# Percent variance explained
pos_eig_1 <- pcoa_result_1$eig[pcoa_result_1$eig > 0]
var_explained_1 <- round(100 * pcoa_result_1$eig[1:2] / sum(pos_eig_1), 1)

# (Redundant with how meta4 was built, but explicit) 
# Remove any samples with NA for symbiont.potential or location
pcoa_points_1 <- pcoa_points_1 |>
  dplyr::filter(!is.na(symbiont.potential),
                !is.na(location))

# Plot: color/fill = symbiont.potential, shape = location
p_1 <- ggplot(
  pcoa_points_1,
  aes(x = PCoA1, y = PCoA2,
      color = symbiont.potential,
      fill  = symbiont.potential)
) +
  geom_point(aes(shape = location), size = 3, alpha = 0.8) +
  stat_ellipse(
    geom   = "polygon",
    alpha  = 0.15,
    level  = 0.95,
    type   = "t",
    colour = NA
  ) +
  labs(
    title = "PCoA of Bray–Curtis Dissimilarity by Symbiont Potential & Location",
    x     = paste0("PCoA1 (", var_explained_1[1], "%)"),
    y     = paste0("PCoA2 (", var_explained_1[2], "%)"),
    fill  = "Symbiont Potential",
    color = "Symbiont Potential",
    shape = "Location"
  ) +
  theme_minimal(base_size = 13) +
  theme(legend.position = "right")

print(p_1)
```

```{r}
table(meta4$symbiont.potential[rownames(permanova_numeric_data4)])

```

## Scleractinia PERMANOVA and PCoA

```{r}
#| label: Scleractinia_only_df

Scleractinia_only <- Corals_clean[Corals_clean$host_order == "Scleractinia", ]
```

```{r}
#| label: Scleractinia_family_PERMANOVA

# 1) select metabolite columns that start with "x"
met_cols <- grep("^x", names(Scleractinia_only), value = TRUE)
if (length(met_cols) == 0) stop("No metabolite columns starting with 'x' found in Scleractinia_only.")

# 2) build numeric metabolite matrix
met_mat <- as.data.frame(Scleractinia_only[, met_cols, drop = FALSE], stringsAsFactors = FALSE)
met_mat[] <- lapply(met_mat, function(x) as.numeric(as.character(x)))  # coerce to numeric

# 3) filter rows: require non-NA in met_mat and host_family
keep <- complete.cases(met_mat) & !is.na(Scleractinia_only$host_family)

# 4) drop samples with zero total abundance (optional but recommended)
met_mat_sub <- as.matrix(met_mat[keep, , drop = FALSE])
row_total <- rowSums(met_mat_sub, na.rm = TRUE)
keep2 <- row_total > 0
if (sum(keep) == 0 || sum(keep & keep2) == 0) stop("No samples left after filtering NA/zero rows.")
met_mat_sub <- met_mat_sub[keep2, , drop = FALSE]

# 5) corresponding metadata subset
meta_sub <- Scleractinia_only[rownames(met_mat_sub), , drop = FALSE]

# 6) Bray-Curtis distance and PERMANOVA
bray_curtis <- vegdist(met_mat_sub, method = "bray")
set.seed(42)
family_permanova_result <- adonis2(bray_curtis ~ host_family, data = meta_sub, permutations = 999, by = "margin")
print(family_permanova_result)
```

```{r}
#| label: PCoA_family

# Perform PCoA 
pcoa_res <- cmdscale(as.dist(bray_curtis), eig = TRUE, k = 2)

# Build plotting dataframe (assumes distance matrix has rownames matching Scleractinia_only)
sample_ids <- rownames(as.matrix(bray_curtis))
coords <- as.data.frame(pcoa_res$points[, 1:2])
colnames(coords) <- c("Axis1", "Axis2")
pcoa_df <- data.frame(Sample = sample_ids,
                      Axis1 = coords$Axis1,
                      Axis2 = coords$Axis2,
                      host_family = as.factor(Scleractinia_only[sample_ids, "host_family"]),
                      row.names = sample_ids)

# % variance explained (use positive eigenvalues)
eig <- pcoa_res$eig
pos_sum <- sum(eig[eig > 0])
var_explained <- if (pos_sum > 0) round(100 * eig / pos_sum, 1) else round(100 * eig / sum(abs(eig)), 1)

# Plot
ggplot(pcoa_df, aes(x = Axis1, y = Axis2, color = host_family, fill = host_family)) +
  geom_point(size = 3) +
  stat_ellipse(type = "t", level = 0.95, alpha = 0.2) +
  labs(
    title = "PCoA by Family for Scleractinia",
    x = paste0("PCoA1 (", var_explained[1], "%)"),
    y = paste0("PCoA2 (", var_explained[2], "%)"), 
    color = "Family", 
    fill = "Family"
  ) +
  theme_minimal()

```

```{r}
#| label: Scleractinia_genus_PERMANOVA

# 1) locate metabolite columns
met_cols <- grep("^x", names(Scleractinia_only), value = TRUE)
if (length(met_cols) == 0) stop("No metabolite columns starting with 'x' found in Scleractinia_only.")

# 2) build numeric matrix (coerce safely)
met_df <- as.data.frame(Scleractinia_only[, met_cols, drop = FALSE], stringsAsFactors = FALSE)
met_df[] <- lapply(met_df, function(x) as.numeric(as.character(x)))  # turns factors/chars -> numeric (NAs if non-numeric)

# 3) remove rows with any NA in metabolite columns or NA in host_genus
keep <- complete.cases(met_df) & !is.na(Scleractinia_only$host_genus)
met_df_sub <- met_df[keep, , drop = FALSE]
meta_sub <- Scleractinia_only[keep, , drop = FALSE]
if (nrow(met_df_sub) == 0) stop("No rows remain after removing NAs. Inspect metabolite columns or host_genus for missing data.")

# 4) remove rows with zero total abundance (vegdist/Bray needs non-zero rows)
row_tot <- rowSums(met_df_sub, na.rm = TRUE)
keep2 <- row_tot > 0
if (!any(keep2)) stop("All remaining samples have zero total metabolite abundance.")
met_mat <- as.matrix(met_df_sub[keep2, , drop = FALSE])
meta_sub <- meta_sub[keep2, , drop = FALSE]

# 5) Bray-Curtis and PERMANOVA
bray_curtis <- vegdist(met_mat, method = "bray")
set.seed(42)
res_genus <- adonis2(bray_curtis ~ host_genus, data = meta_sub, permutations = 999, by = "margin")
print(res_genus)
```

```{r}
#| label: PCoA_genus_Scleractinia

# Perform PCoA 
pcoa_res <- cmdscale(as.dist(bray_curtis), eig = TRUE, k = 2)

# Build plotting dataframe (assumes distance matrix has rownames matching Scleractinia_only)
sample_ids <- rownames(as.matrix(bray_curtis))
coords <- as.data.frame(pcoa_res$points[, 1:2])
colnames(coords) <- c("Axis1", "Axis2")
pcoa_df <- data.frame(Sample = sample_ids,
                      Axis1 = coords$Axis1,
                      Axis2 = coords$Axis2,
                      host_genus = as.factor(Scleractinia_only[sample_ids, "host_genus"]),
                      row.names = sample_ids)

# % variance explained (use positive eigenvalues)
eig <- pcoa_res$eig
pos_sum <- sum(eig[eig > 0])
var_explained <- if (pos_sum > 0) round(100 * eig / pos_sum, 1) else round(100 * eig / sum(abs(eig)), 1)

# Plot
ggplot(pcoa_df, aes(x = Axis1, y = Axis2, color = host_genus, fill = host_genus)) +
  geom_point(size = 3) +
  stat_ellipse(type = "t", level = 0.95, alpha = 0.2) +
  labs(
    title = "PCoA by Genus for Scleractinia",
    x = paste0("PCoA1 (", var_explained[1], "%)"),
    y = paste0("PCoA2 (", var_explained[2], "%)"), 
    fill = "Genus", 
    color = "Genus"
  ) +
  theme_minimal()

```

## General PERMANOVA for Corals vs other

```{r}
#| label: PCoA_plot_based_on_Jaccard_distances

pcoa <- cmdscale(jaccard_dist, eig = TRUE, k = 2)
plot_df <- data.frame(
  Axis1 = pcoa$points[,1],
  Axis2 = pcoa$points[,2],
  group = df$group_hexocto
)

ggplot(plot_df, aes(x = Axis1, y = Axis2, color = group)) +
  geom_point(size = 3, alpha = 0.7) +
  theme_minimal(base_size = 14) +
  labs(title = "PCoA of Metabolite Presence (Jaccard)",
       subtitle = "Hexacorallia + Octocorallia vs Other",
       x = "PCoA Axis 1", y = "PCoA Axis 2")

```

```{r}
# PCoA plot of metabolite abundances (Hellinger + Bray–Curtis)
library(ggplot2)

# PCoA on Bray–Curtis distance
pcoa <- cmdscale(bc_dist, eig = TRUE, k = 2)

# Build plotting dataframe
plot_df <- data.frame(
  Axis1 = pcoa$points[, 1],
  Axis2 = pcoa$points[, 2],
  group = df_sub$group_hexocto
)

# Percent variance explained by first two axes
var_expl <- round(100 * pcoa$eig[1:2] / sum(pcoa$eig[pcoa$eig > 0]), 1)

# Plot
ggplot(plot_df, aes(x = Axis1, y = Axis2, color = group)) +
  geom_point(size = 3, alpha = 0.8) +
  theme_minimal(base_size = 14) +
  labs(
    title = "PCoA of Metabolite Abundances (Bray–Curtis)",
    x = paste0("PCoA1 (", var_expl[1], "%)"),
    y = paste0("PCoA2 (", var_expl[2], "%)"),
    color = "Group"
  ) +
  stat_ellipse(level = 0.95, linetype = "dashed")

```

```{r}
genus_counts <- table(Corals_clean$host_genus)
genus_counts <- sort(genus_counts, decreasing = TRUE)
genus_counts
```
